#!/bin/sh
# Loop Browser commit guard — blocks committing private/secret data (the git repo IS
# the distribution channel). Installed automatically by `npm install` (package.json
# "prepare" sets core.hooksPath). Emergency bypass: git commit --no-verify
node scripts/check-no-secrets.mjs --staged
